# Manifest Created by Bagas Maulana
# Reference: Aji Diyantoro Youtube

apiVersion: v1
kind: Service
metadata:
  name: access
spec:
  type: NodePort
  selector:
    app: nginx
  ports:
    - port: 8080
      targetPort: 80
      protocol: TCP